<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReDi">
        <activity
            android:name=".user.activities.OrdersActivity"
            android:exported="false" />
        <activity
            android:name=".user.activities.CartActivity"
            android:exported="false" />
        <activity
            android:name=".user.activities.AccountActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".auth.SplashActivity" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".admin.activities.MainAdminActivity"
            android:exported="false" />
        <activity
            android:name=".user.activities.MainUserActivity"
            android:exported="false" />
        <activity
            android:name=".auth.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".auth.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".auth.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>